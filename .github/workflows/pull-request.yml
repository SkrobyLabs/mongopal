name: Pull Request Build

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      publish: false
